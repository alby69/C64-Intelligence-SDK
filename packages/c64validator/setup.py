from setuptools import setup, find_packages

setup(
    name="c64validator",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    author="Alberto Abate",
    description="Simulazione e validazione 6502",
)
