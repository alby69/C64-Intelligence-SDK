from setuptools import setup, find_packages

setup(
    name="c64debugger",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    author="Alberto Abate",
    description="Breakpoints e connessione socket a VICE",
)
