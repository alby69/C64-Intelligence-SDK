from setuptools import setup, find_packages

setup(
    name="c64extractor",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Alberto Abate",
    description="C64 file formats extraction tools (PRG, D64, G64)",
)
